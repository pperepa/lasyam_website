name: sandbox-build-checks
on: 
  push: 
    branches:
      - sandbox
jobs:
  checkout_github_repository:
    runs-on: ubuntu-latest
    steps:
      - name: checkout-main-branch
        uses: actions/checkut@v3
        with:
          ref: 'sandbox'

      # - name: ssh-key-init
      # - uses: webfactory/ssh-agent@v0.5.4
      #   with:
      #     ssh-private-key: ${{secrets.SSH_PRIVATE_KEY}}
      #     ref: 'main'
